<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Bienvenido!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}
            {{ encore_entry_script_tags('user') }}#}
        {% endblock %}
    </head>
    <body>
        {% block body %}
            <h1>Primer curso de Symfony</h1>

               

                {% for nro in 1..20 %}  {#punto 3a, del trabajo practico 2#} 

                        {% if nro == 1  %} {#punto 3b, del trabajo practico 2#}                      
                         <div>Título Principal del Curso</div>        
                        {% endif %}

                    <div>{{ 'curso nro'|upper}} {{nro}} {{'de symfony'|upper }}</div> {#punto 3a, del trabajo practico 2#}   
                
                {% endfor %}

        {% endblock %}
    </body>
</html>
